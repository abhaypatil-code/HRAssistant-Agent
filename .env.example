# ============================================================================
# HR Assistant Agent - Environment Configuration
# ============================================================================
# This file contains all environment variables needed to run the application.
# Copy this file to .env and fill in your actual values.
#
# SECURITY WARNING: Never commit the .env file to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# Google Gemini API Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Get your free API key at: https://aistudio.google.com/app/apikey
# 
# For production use:
# - Use a separate API key from development
# - Consider upgrading to a paid tier for higher quotas
# - Monitor usage at: https://makersuite.google.com/app/apikey
# - Set up billing alerts to avoid unexpected charges
#
# API Key format: Typically starts with "AIza..."
GOOGLE_API_KEY=your_gemini_api_key_here

# ----------------------------------------------------------------------------
# Model Configuration (Optional)
# ----------------------------------------------------------------------------
# Specify which Gemini model to use. Options:
# - gemini-2.0-flash (default, fast and cost-effective)
# - gemini-pro (higher quality, slower, more expensive)
# - gemini-1.5-flash (previous generation)
#
# Uncomment and modify if you want to override the default:
# MODEL_NAME=gemini-2.0-flash

# ----------------------------------------------------------------------------
# LLM Parameters (Optional)
# ----------------------------------------------------------------------------
# TEMPERATURE: Controls randomness in responses (0.0 to 1.0)
#   - Lower (0.0-0.3): More deterministic, factual responses
#   - Medium (0.4-0.7): Balanced creativity and consistency
#   - Higher (0.8-1.0): More creative, varied responses
# Default: 0.7
# TEMPERATURE=0.7

# MAX_TOKENS: Maximum length of generated responses
#   - Typical range: 512-2048
#   - Higher values = longer responses but slower and more expensive
# Default: 1024
# MAX_TOKENS=1024

# ----------------------------------------------------------------------------
# RAG (Retrieval Augmented Generation) Settings (Optional)
# ----------------------------------------------------------------------------
# CHUNK_SIZE: Number of characters per document chunk
#   - Smaller chunks: More precise but may lose context
#   - Larger chunks: More context but less precise
#   - Recommended range: 500-1500
# Default: 1000
# CHUNK_SIZE=1000

# CHUNK_OVERLAP: Number of overlapping characters between chunks
#   - Helps maintain context across chunk boundaries
#   - Typically 10-20% of CHUNK_SIZE
# Default: 200
# CHUNK_OVERLAP=200

# RETRIEVAL_K: Number of document chunks to retrieve for each query
#   - More chunks: More context but slower and more tokens used
#   - Fewer chunks: Faster but may miss relevant information
#   - Recommended range: 3-6
# Default: 4
# RETRIEVAL_K=4

# SIMILARITY_THRESHOLD: Minimum similarity score for retrieved chunks (0.0 to 1.0)
#   - Higher values: Only highly relevant chunks
#   - Lower values: More permissive retrieval
# Default: 0.5
# SIMILARITY_THRESHOLD=0.5

# ----------------------------------------------------------------------------
# File Paths (Optional - Advanced)
# ----------------------------------------------------------------------------
# Override default file paths if you're using custom directory structure
# Paths should be relative to project root or absolute

# Employee data CSV file location
# EMPLOYEE_DATA_PATH=data/employee_data.csv

# Directory containing HR policy documents
# POLICIES_DIR=data/policies

# Directory for storing FAISS vector store
# VECTOR_STORE_PATH=data/vector_store

# ----------------------------------------------------------------------------
# Streamlit Configuration (Optional)
# ----------------------------------------------------------------------------
# Custom page title and icon for browser tab
# PAGE_TITLE=HR Copilot AI Agent
# PAGE_ICON=👔

# ----------------------------------------------------------------------------
# Logging Level (Optional)
# ----------------------------------------------------------------------------
# Controls verbosity of application logging
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# Production Environment Variables (Optional)
# ----------------------------------------------------------------------------
# These are typically used in production deployments

# Application environment (development, staging, production)
# ENVIRONMENT=development

# Enable/disable debug mode
# DEBUG=False

# Custom port for Streamlit (default: 8501)
# STREAMLIT_PORT=8501

# Max upload file size in MB (default: 200MB)
# MAX_UPLOAD_SIZE=200

# Session timeout in minutes (Streamlit Community Cloud)
# SESSION_TIMEOUT=30

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# 1. After copying this file to .env, remove the comments to keep it clean
# 2. Never share your .env file or commit it to Git
# 3. For production, use a secret management service (AWS Secrets Manager, etc.)
# 4. Restart the application after changing environment variables
# 5. Use .env.local for local overrides (add to .gitignore)
# ============================================================================
